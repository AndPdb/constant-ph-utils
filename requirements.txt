# constant-ph-utils runtime dependencies
# Requires Python >= 3.10 (analyses.py annotates with `int | slice | tuple`)
#   pip install -r requirements.txt

numpy>=1.20
pandas>=1.3
matplotlib>=3.4        # bar_label

# Optional: shell tab-completion for run_analysis.py flags.
# The CLI runs fine without it.
#   pip install argcomplete